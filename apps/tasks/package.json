{
  "name": "@mustaka/tasks",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/tasks/index.ts"
  },
  "scripts": {
    "dev": "bun run --env-file ../../.env --watch src/index.ts",
    "start": "bun run --env-file ../../.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@mustaka/db": "workspace:*",
    "@mustaka/storage": "workspace:*",
    "pg-boss": "^12.32.0",
    "zod": "^4.6.5"
  }
}
