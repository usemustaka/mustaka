{
  "name": "@mustaka/api",
  "version": "1.0.50",
  "module": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --env-file ../../.env --watch src/index.ts"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.82",
    "@elysiajs/cors": "^1.4.2",
    "@mustaka/auth": "workspace:*",
    "@mustaka/db": "workspace:*",
    "@mustaka/logger": "workspace:*",
    "@mustaka/storage": "workspace:*",
    "@mustaka/tasks": "workspace:*",
    "@zenstackhq/server": "^3.9.4",
    "ai": "^7.0.102",
    "elysia": "^1.4.30",
    "pg-boss": "^12.32.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "bun-types": "^1.4.2"
  }
}
